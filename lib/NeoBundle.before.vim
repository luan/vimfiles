" ----------------------------------------
" NeoBundle requirements
" ----------------------------------------
set nocompatible               " Be iMproved

if has('vim_starting')
  set runtimepath+=$HOME/.vim/bundle/neobundle.vim/
endif

